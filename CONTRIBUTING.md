# 参与开发

先建立 Python 虚拟环境，执行 `python -m pip install -e .`，再运行 `python -m unittest discover -s tests -v`。构建原生 host 使用 `src/autoshop_mcp/native/build.cmd`。

修改工程处理逻辑时，应检查原文件不变、配置字节保留、路径边界、旧产物隔离和失败结果。修改原生接口时，使用自己的离线工程运行集成测试，并记录 DLL 指纹、基准机器码比较、错误指令拒绝和配置检查结果。不要把静态检查或模拟结果写成原厂编译通过。

新增版本适配需要同时检查 Python profile、原生指纹、内部对象布局、接口调用和编译输出。仅修改哈希不可作为适配提交。

提交内容限原创源码、合成测试和可公开文档。不要提交原厂 DLL、安装包、现场程序、密码、客户信息、个人路径或构建日志中的私有数据。问题报告可删去私有路径后附错误码、工具版本和最小复现步骤。
